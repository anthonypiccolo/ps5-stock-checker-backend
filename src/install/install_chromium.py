#use this to setup your chromium environment to stable builds which match
# #does not seem to work
# from selenium_driver_updater import DriverUpdater
# import os


# print("attempting to update chrome driver and binary to compatible builds...")

# base_dir = "/app/local/" #os.path.dirname(os.path.abspath(__file__))
# list_drivers = [DriverUpdater.chromedriver] #, DriverUpdater.geckodriver]

# filename = DriverUpdater.install(path=base_dir, driver_name=list_drivers, upgrade=True, check_driver_is_up_to_date=True)
# print(filename)
# print("complete")